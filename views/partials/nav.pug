nav(class="mainNav navbar navbar-default navbar-fixed-top nav-"+pageType, id="mainNav")
    .container
        // Brand and toggle get grouped for better mobile display
        .navbar-header
            button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1')
                span.sr-only Toggle navigation
                |  Menu
                i.fa.fa-bars
            a.navbar-brand.page-scroll(href='#page-top')
                img(src='img/dawn_logo_final.png', alt='Dawn: Distributed Autonomous Worldwide Network')
        // Collect the nav links, forms, and other content for toggling
        #bs-example-navbar-collapse-1.collapse.navbar-collapse
            ul.nav.navbar-nav.navbar-right
                //
                  <li>
                  <a class="page-scroll" href="#download">Whitepaper</a>
                  </li>
                li
                    if(pageType=="home")
                        a.page-scroll(href='#features') Features
                    else
                        a.page-scroll(href='index.html#features') Features
                li
                    if(pageType == "home")
                        a.page-scroll(href='#contact') Contact
                    else
                        a.page-scroll(href='index.html#contact') Contact

                li
                    if(pageType == "whitepaper")
                        a.page-scroll(href='#introduction') Whitepaper
                    else
                        a.page-scroll(href='whitepaper.html') Whitepaper

                //
                  <li>
                  <a class="page-scroll" href="#ico">ICO</a>
                  </li>
        // /.navbar-collapse
    // /.container-fluid